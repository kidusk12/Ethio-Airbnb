/**
 * Centralized business constants.
 * Change values here — they propagate everywhere automatically.
 */

/** Host commission rate (15%). Platform keeps this fraction of each booking. */
export const HOST_COMMISSION_RATE = 0.15;
